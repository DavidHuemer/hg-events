import {EquipmentFilePipe} from './equipment-file.pipe';

describe('EquipmentFilePipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentFilePipe();
    expect(pipe).toBeTruthy();
  });
});
